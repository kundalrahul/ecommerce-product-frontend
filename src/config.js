// const config = {
//   API_BASE_URL: "http://localhost:8080", // Update this to match your backend URL
// };

const config = {
  API_BASE_URL:
    "https://product-backend-service-96497154465.us-central1.run.app", // Update this to match your backend URL
};

export default config;
